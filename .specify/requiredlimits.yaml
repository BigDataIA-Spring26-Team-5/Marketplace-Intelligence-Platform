# Production Scale Limits Configuration
# These limits govern the pipeline's production-scale behavior and can be adjusted per-deployment

max_llm_calls_per_run: 500
max_s3_calls_per_chunk: 50
llm_confidence_threshold: 0.85
chunk_size: 5000
checkpoint_interval: 1000